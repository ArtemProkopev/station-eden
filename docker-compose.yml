services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: eden
      POSTGRES_PASSWORD: edenpwd
      POSTGRES_DB: eden
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data:Z
volumes:
  pgdata: {}
