[package]
name = "username-generator"
version = "0.2.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
path = "lib.rs"

[dependencies]
wasm-bindgen = "0.2"
js-sys = "0.3"
web-sys = { version = "0.3", features = ["Window", "Crypto"] }

[profile.release]
lto = true
opt-level = "s"
codegen-units = 1
