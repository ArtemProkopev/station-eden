{
  "name": "@station-eden/username-generator",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rimraf ../../apps/web/public/wasm",
    "build": "wasm-pack build --release --target web --out-dir ../../apps/web/public/wasm"
  }
}
